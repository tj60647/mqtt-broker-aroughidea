# Secrets
config/passwords
certs/*.key
certs/*.crt
certs/*.srl
config/certs/*.key
config/certs/*.crt
config/certs/*.srl

# User-generated config (copy from acl.example to create)
config/acl

# Runtime state
data/
log/

# OS / editor noise
.DS_Store
Thumbs.db
.vscode/
